// Compatibility entrypoint. Every Studio path now uses the same planner and renderer.
export { planImage, buildRenderPrompt } from './studio/prompts';
export { StudioService } from './studio/service';
